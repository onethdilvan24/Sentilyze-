name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  ci:
    name: Lint, Type Check & Build
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/sentilyze/package-lock.json

      - name: Install dependencies
        working-directory: frontend/sentilyze
        run: npm ci

      - name: Run ESLint
        working-directory: frontend/sentilyze
        run: npm run lint

      - name: TypeScript type check
        working-directory: frontend/sentilyze
        run: npx tsc --noEmit --skipLibCheck

      - name: Build application
        working-directory: frontend/sentilyze
        run: npm run build
